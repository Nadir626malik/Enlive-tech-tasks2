function createCounter() {
  let count = 0;
  return {
    inc: () => ++count,
    dec: () => --count,
    get: () => count
  };
}

const c1 = createCounter();
const c2 = createCounter();
console.log(c1.inc()); // 1
console.log(c2.inc()); // 1 (independent)