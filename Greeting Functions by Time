function greetingFactory(hour) {
  if (hour < 12) return () => "Good Morning!";
  if (hour < 18) return () => "Good Afternoon!";
  return () => "Good Evening!";
}

let greet = greetingFactory(new Date().getHours());
console.log(greet());